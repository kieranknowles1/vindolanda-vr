%% Commands for TeXCount
%TC:macro \cite [option:text,text]
%TC:macro \citep [option:text,text]
%TC:macro \citet [option:text,text]
%TC:envir table 0 1
%TC:envir table* 0 1
%TC:envir tabular [ignore] word
%TC:envir displaymath 0 word
%TC:envir math 0 word
%TC:envir comment 0 0
\documentclass[sigconf,authordraft]{acmart}

\usepackage{svg}
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small
}

\usepackage{subfig}

\author{Kieran John Knowles}
\affiliation{%
  \institution{Newcastle University}
  \city{Newcastle Upon Tyne}
  \country{United Kingdom}
}

\setcopyright{none}
\copyrightyear{2025}
\begin{document}

\title{Vindolanda VR}

\begin{abstract}
  Virtual reality is an exciting emerging field with many potential applications,
  from games to immersive experiences. This paper explores the potential of VR
  in teaching the history of Roman Britain at Vindolanda through the creation of
  a virtual tour designed to work for both first-time and experienced VR users.
\end{abstract}

\begin{CCSXML}
<ccs2012>
    <concept>
        <concept_id>10010405.10010489.10010491</concept_id>
        <concept_desc>Applied computing~Interactive learning environments</concept_desc>
        <concept_significance>300</concept_significance>
        </concept>
    <concept>
        <concept_id>10010405.10010476.10011187.10011190</concept_id>
        <concept_desc>Applied computing~Computer games</concept_desc>
        <concept_significance>500</concept_significance>
        </concept>
    <concept>
        <concept_id>10011007.10011074.10011075</concept_id>
        <concept_desc>Software and its engineering~Designing software</concept_desc>
        <concept_significance>100</concept_significance>
        </concept>
  </ccs2012>
\end{CCSXML}

\ccsdesc[300]{Applied computing~Interactive learning environments}
\ccsdesc[500]{Applied computing~Computer games}
\ccsdesc[100]{Software and its engineering~Designing software}

\keywords{Virtual reality, History, Education, Serious games}

\begin{teaserfigure}
  \includegraphics[width=\textwidth]{teaser.png}
  \caption{In-game depiction of Vindolanda viewed from overhead.}
  \Description{The in-game depiction of vindolanda showing the fort's exterior
  walls, barracks, granary, and headquarters.}
  \label{fig:teaser}
\end{teaserfigure}

\maketitle

\section*{Links}
\noindent Demo Video: \url{https://youtu.be/CaM-xLr3iG0}

\noindent \hangindent=2em The Bugs: \href{https://www.youtube.com/playlist?list=PL1UYxx9QvwZrJnvfPb9MwA13YsFu4lFUf}{
  https://www.youtube.com/playlist?list=\\PL1UYxx9QvwZrJnvfPb9MwA13YsFu4lFUf
}

\noindent GitHub: \url{https://github.com/kieranknowles1/vindolanda-vr/}

\noindent itch.io: \url{https://kieranknowles.itch.io/vindolanda-vr}

\section{Introduction}

Roman occupation of Britain and Hadrian's wall plays a key part in the history
of Britain, particularly in the North-East of England. This seemed a natural
area to create a game to both educate children on history and introduce
them to the world of virtual reality (VR). The decision to use VR over a
traditional display (flatscreen) raises a number of unique challenges in
managing motion sickness, safety due to spatial awareness, and the requirement
to use motion controls instead of keyboard mappings to interact with the world.

Artistic license was taken to ease development by giving more reference material
as the ninth fort, despite not existing during Hadrian's rule, is the best
documented and preserved of the forts.

The VR ecosystem is highly fragmented, with multiple competing headsets and many
more controllers, each featuring different inputs which adds challenge to
creating input schemes compatible with different hardware, this area was deemed
impractical to address as it would require bespoke testing for each type of
controller.

The author's prior experience with playing, but not developing, VR games gave a
base level of knowledge in VR interactions such as picking up and using items,
as well as in movement types, but also limited measurement of how intuitive
these are for new users.

\section{Background \& Related Work}

\subsection{Historical Research}

To accurately recreate a Roman fort, it was essential to first gain an understanding
of Roman Britain. However, this was not a focus of this paper and as such most
research was focused on the technology to be used.

\subsubsection{Site Visits}

\paragraph{\label{sec:site_visit}Vindolanda}
A visit to the Vindolanda Museum was made in order to gather reference
material. This gave valuable insight into the layout of the fort and its
composition. Due to time and weather constraints, Castle Nick and Hadrian's Wall were not visited
and thus had fewer references to work from.

\paragraph{Hancock Museum}

In addition to the fort itself, a visit was made to the Roman exhibits at the
Hancock Museum to gather reference material for the wall and the Roman soldiers.
This included the armour shown in Figure~\ref{fig:armour} and a milecastle that
would be built every Roman mile ($\sim{}1.5$km) \cite[p.1027]{smith_new_1851} shown in
Figure~\ref{fig:milecastle}. These gave inspiration from which their in-game
depictions could be based on.

\begin{figure}[ht]
  \includegraphics[width=0.8\linewidth]{armour.jpg}
  \caption{\label{fig:armour} A replica of an auxiliary cavalryman's armour
    (left), 2nd century AD. And a Roman legionary's (right), late 1st
    century AD.
  Via Hancock Museum.}
  \Description{Models Roman auxiliary and legionary with shields and spears.}
\end{figure}

\begin{figure}[ht]
  \includegraphics[width=0.8\linewidth]{milecastle.jpg}
  \caption{\label{fig:milecastle}A model of a milecastle from Hadrian's Wall.
  Via Hancock Museum}
  \Description{A scale model of a Roman milecastle from Hadrian's Wall, showing
  a square fortification enclosing buildings and a gate.}
\end{figure}

\subsubsection{Literature}

The books ``The wall: Rome's greatest frontier'' \cite{moffat_wall_2009} and
``Hadrian's empire: when Rome ruled the world'' \cite{danziger_hadrians_2006}
gave valuable insight into the daily lives of both Romans and Briton's during
Hadrian's rule which was used as a high-level overview of the subject and avenue
for future research.

\subsection{Technical Research}

\subsubsection{Which Engine?}

Multiple engines were considered, each with their own benefits
and drawbacks as shown in Table \ref{table:engine_compare}

\begin{table*}
  \caption{The advantages and disadvantages of the considered engines.}
  \label{table:engine_compare}
  \begin{tabular}{llll}\toprule
    Feature & Unity & Unreal & Godot \\\midrule

    Language & C\# & Blueprints, C++ & GDScript, C++, C\#, etc. \\
    VR Support & De facto standard & Performance concerns & Yes \\
    Recent VR Games & $745 (70\%)$ & $351 (30\%)$ & $8 (<1\%)$ \\
    Version Control & Git, text & Perforce, binary & Git, text \\
    Linux Support & Yes & Editor unreliable & Yes \\

    \bottomrule
  \end{tabular}
\end{table*}

Virtual reality is a significantly more demanding environment than other games.
While 1080p at 60Hz is a common target for flat screens, VR games must render
two screens at 90-120Hz each, with users often upscaling for enhanced clarity.
Consequently, performance is a crucial requirement for any VR game.
(\hyperref[sec:nfr_performance]{NFR01})

Given the short deadline of the project, industry adoption was also a major
consideration based on the assumption that a more widely adopted engine would
have more resources available to reduce development time. The total number of
games tagged as VR, detected as using a specific engine by
SteamDB (\url{https://steamdb.info/tech/}), and released
after May 2023 (to include current trends) were used to determine adoption.
This methodology has its limitations: by only using SteamDB as a source,
selection bias is introduced as non-steam releases such as itch.io and the Meta
Store are excluded. Additionally, SteamDB's engine detection can produce false
negatives, for example, Godot games can be packaged as a single executable which
will not be detected as shown in Figure~\ref{fig:godot_false_negative}. Despite
these limitations, this methodology was considered good enough to get a
high-level overview.

\begin{figure}[ht]
  \begin{lstlisting}
    $ ./kicksalot --help
    Godot Engine v4.4 - https://godotengine.org
    ... (output abridged)
  \end{lstlisting}
  \includegraphics[width=0.8\linewidth]{godot-false-negative.png}
  \caption{\label{fig:godot_false_negative} A game that uses Godot, but is not
    detected as such by SteamDB visible from the lack of a ``Technologies''
  field. https://steamdb.info/app/2629230/}
  \Description{A screenshot of SteamDB's entry for ``The Adventures of Sir
  Kicksalot''}
\end{figure}

A smaller, but important, consideration was the engine's support for Git version
control. While it would have been possible to install and learn Perforce, doing
so would detract from development time, this made Unreal Engine a less practical
choice as it uses binary files for assets, which Git is poorly suited at
versioning.

Based on this analysis, it was determined that Unity best fits the needs of the
game as it is an industry standard and provides a VR template that includes all
locomotion types needed and integration with its UI system. More specifically,
Unity 6.1 was used as it was the latest stable version at time of development.

\subsubsection{Which Controllers?}

There are many distinct controllers available for VR systems with significant
variance in the inputs available, their locations, and tracking quality. Fully
supporting each would require bespoke assets for tutorials along with testing,
which is impractical due to their high cost. Therefore, only the Valve Index
controllers are fully supported with Unity and SteamVR's remapping used to
handle other controller types.

\subsubsection{\label{sec:vr_refs}VR In Other Games}

Implementations of VR in other games, especially with how they handle
locomotion was analysed as inspiration for how it could be handled here. Two
primary methods were identified that would be practical here:

\begin{itemize}
  \item Teleport - Player uses the joystick to select a location, then releases
    to teleport. Suggested to inexperienced users.
  \item Smooth (Head Forward) - Player holds the joystick to move forward relative
    to their headset. Can be more immersive but is also nauseating for inexperienced
    users. Players looking around the scene can also introduce nausea as their movement
    direction changes unexpectedly.
  \item Smooth (Controller Forward) - Similar to head forward, except movement
    is relative to the controller's facing. This can be less disorienting when
    exploring, but is less suited for action-packed areas where players frequently
    need to reach behind their back, such as to retrieve an arrow from a quiver.
\end{itemize}

Given the variety of preferences among users, locomotion types should be
configurable such that allow each user may select what they are most comfortable
with, as well as to add vignette based on their needs. A sketch of the locomotion
settings UI is shown in Figure~\ref{fig:locomotion_settings}. While some games include
more advanced movement such as climbing or jumping, these were not deemed
necessary here. Both smooth movement and teleportation are supported by Unity's VR
system, and therefore were trivial to implement as specified in \hyperref[sec:fr_teleport]{FR04}
and \hyperref[sec:fr_smooth]{FR05}.

\begin{figure}[ht]
  \includesvg[width=\linewidth]{locomotion-ui-sketch.svg}
  \caption{\label{fig:locomotion_settings}Sketch for the locomotion settings UI.}
  \Description{
    An interface sketch showing options to move via teleporting or smootly,
    with a vignette slider that is only enabled when using smooth movement and
    a warning that it may cause motion sickness.
  }
\end{figure}

Text rendering in other games was identified as a common weak point, with some,
such as Blade and Sorcery (Figure~\ref{fig:bns_text})
using fonts that are both small and difficult to read. This was a clear area for
improvement here defined as \hyperref[sec:nfr_text]{NFR03} and further detailed
in Section~\ref{sec:text_scale}.

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{text_bns.png}
  \caption{\label{fig:bns_text}Text from Blade and Sorcery that uses a small
  serif font that makes it difficult to read in VR.}
  \Description{A screenshot of an in-game journal showing small print text
  that is difficult to read.}
\end{figure}

\section{Design \& Implementation}

\subsection{The Setting}

While Roman occupation of Britain spanned centuries, it was decided to set this
game during Emperor Hadrian's visit in 122 AD.
\cite[p.176]{danziger_hadrians_2006}, \cite[p.157]{moffat_wall_2009}
This setting gives the opportunity to teach about the rule of Hadrian himself
and the history leading to the construction of Hadrian's Wall, as well as other
events such as the disappearance/defeat of the ninth legion circa 120 AD.
\cite{rocha_romes_2021, hodgson_end_2021}

The DEM.Net elevation API was used to produce a height map of the fort and
surrounding area including satellite imagery. While the resolution was too low
for standalone use (~1 pixel per metre and lower still for elevation), it
provided a suitable backdrop for the scene and guided the scale of the
reproduction.

An OpenStreetMap export served as a reference for the fort's layout which could
be overlaid on the Unity editor with a script and used to guide level design.
This included the fort's exterior walls, its barracks, the granary, headquarters,
commander's quarters, and the town's strip houses and tavern. The depiction of
the wall likewise includes the wall itself and ``Castle Nick''.

\subsubsection{The Fort}

There were at least nine forts built on the site of Vindolanda, the early ones
being built out of untreated timber and demolished when their garrisons were
recalled, with the next fort being built on top of the previous's remains.
\cite{birley_vindolanda_2009} These would be garrisoned by a cohort of ten
centuries totalling eight hundred men, although not all would be present at the
fort and as much as a third would have been unfit for service, likely due to
poor hygiene, as documented in a strength report from circa AD 90.
\cite{bowman_military_1991}

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{osm-vindolandra.png}
  \caption{\label{fig:osm_map}Map of Vindolanda. Via OpenStreetMap [ODbL License]}
  \Description{A map of the Vindolanda excavations showing the layout of the
  ninth fort}
\end{figure}

The ninth fort was constructed from stone and, although it didn't exist until
a nearly a century after Hadrian's reign and the setting of this game, it is the
best documented. As such, it and the adjacent town was chosen as the basis for
this depiction, with stone materials being replaced with the more
period-appropriate wood walls and thatch roofs to compromise between ease of
development and historical accuracy. An overhead map of the fort is shown in
Figure~\ref{fig:osm_map}.

The fort's implementation made heavy use of Loafbrr's public domain castle wall
kit with wooden and thatch textures from OpenGameArt, also in the public domain.
These were used to create a reusable set of prefab templates for common areas
such as walls and rooms shown in Figure~\ref{fig:lego_fort}, that were then combined
to form larger structures such as the outer wall or a barracks. This resulted in
fairly detailed, but very similar looking, structures that were quick to lay out.
A small amount of variety was introduced using scripts to randomly select and
position decorative objects such as items on tables. An overhead view of the
final fort is shown in Figure~\ref{fig:teaser}.

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{lego_fort.png}
  \caption{\label{fig:lego_fort}A collection of prefabs used for the fort's
  exterior wall. Top: Gate with watchtowers, bottom left: watchtower, bottom right:
  straight wall.}
  \Description{A set of wall part prefabs for use in the fort.}
\end{figure}

OpenStreetMap's data on the fort and town's layout was used as a reference throughout
with a script that display an editor overlay on the fort scene to base geometry
on. This proved a useful guide, with the limitation of only covering excavated areas.
This factor that is especially limiting in the north-east corner of the site visible
by the lack of detail in Figure~\ref{fig:osm_map}.

To showcase how the fort appears in the present day, partially fufilling
\hyperref[sec:fr_beforeafter]{FR03}, several displays (Figure~\ref{fig:barracks_today})
are included showing photos taken during the fort visit (Section~\ref{sec:site_visit}).

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{barracks_today.png}
  \caption{\label{fig:barracks_today}A photo panel showing the present-day ruins of the barracks.}
  \Description{A screenshot showing a floating panel that shows the ruins of the barracks at Vindolanda.}
\end{figure}

\subsubsection{The Wall and Milecastle}

In addition to the fort, Hadrian's wall through
milecastle 39, frequently referred to as ``Castle Nick'', although it is unclear
where this nickname originated. This site is also notable as being where the famous
Sycamore Gap tree once stood.

Milecastle 39 is situated approximately 1.5 kilometres north-north-west of the fort,
making it impractical to have users manually walk this distance in VR. Instead,
both Vindolanda and Castle Nick feature triggers, shown in Figure~\ref{fig:teleport_trigger}
that directly teleport users between the sites to save them from making the trip themselves.
A birds-eye view of the milecastle and a section of Hadrian's wall is shown in
Figure~\ref{fig:milecastle_over}.

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{teleport-trigger.png}
  \caption{\label{fig:teleport_trigger}A trigger that teleports the user to a linked
  destination when entered.}
  \Description{A cubic green bounding box wireframe representing the trigger area
  with the floating text ``To Vindolanda'' inside it.}
\end{figure}

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{milecastle_over.png}
  \caption{\label{fig:milecastle_over}Birds-eye view of Castle Nick and the connected wall.}
  \Description{The depiction of Castle Nick and Hadrian's Wall as viewed from overhead.}
\end{figure}

\subsection{VR Considerations}
Virtual reality introduces a unique set of challenges to game development. One
of these is motion sickness, especially for users inexperienced with VR,
\cite{chattha_motion_2020} which is often worsened by smooth locomotion, but can
be mitigated by instead using teleport locomotion, although some users may feel
disoriented after teleporting, the majority find it an easy to use movement
method. \cite{bozgeyikli_point_2016} Due to the target audience of this game
being mostly users without prior VR experience, teleport movement was deemed a
higher priority than smooth movement.

\label{sec:text_scale}
Another challenge, especially when using lower resolution headsets, is the
readability of text. Users require relatively large characters of approximately
20-25mm per metre of distance (distance independent millimetre or dmm
\cite{google_for_developers_designing_2017}) to read comfortably on the lower
resolution HTC Vive, \cite{solum_readability_2019} which is the only headset
available to the researcher and limits text to roughly 8 words per line.

Even after taking precautions to limit motion sickness, some sickness is still
to be expected. Coupled with how physically demanding VR can be compared to
other games, \hyperref[sec:fr_save]{FR07} was derived to allow for users to take
breaks as required, which they are instructed to do often and to take things
slow to reduce sickness.

Finally, there is the safety aspect to consider, namely real world boundaries and
obstacles which users may lose awareness of while in VR. Existing boundary systems
which overlay a grid when players get too close to an obstacle, remain the industry
standard and are provided automatically by platforms such as Valve's SteamVR.
\cite{valve_software_steam_nodate} and Meta's Quest \cite{meta_set_nodate}. While
these are less immersive \cite{george_invisible_2020, sanchez-vives_presence_2005}
and increase cognitive load \cite{wickens_multiple_2008} they remain effective
enough to allow safe usage of VR even in fairly small spaces. The tutorial will
be designed to inform users of this to account for players who have not used
their platform's native tutorials.

Testing was complicated by the developer's prior experience which caused
movement options and interactions to appear more intuitive than they may be and
for these systems to cause less motion sickness. This also introduced bias:
while smooth rotation may be acceptable for some users, it was not considered
here due to causing the developer significant disorientation.


\subsection{Educational Value}
Educational games are a subcategory of ``Serious Games'', which aim to use the
medium of games to teach through entertainment, and have had positive results in
prior studies, although there is limited data for their efficacy in teaching
history. \cite{backlund_educational_2013}

Prior research in VR for education has focused largely on university and adult
training with little focus on teaching children due to their more limited 3D
vision and hand-eye coordination meaning its use has to be limited to avoid
health risks. \cite{freina_literature_2015} However, it
also gives the opportunity to improve understanding about areas that
learners would physically be unable to reach such as the solar system, which
would likely be applicable to history. \cite{detlefsen_cosmic_2014}

\subsubsection{Fort Tour}

A tour is used to teach users about the fort's layout, the function of each building,
and other information about Roman Britain. This is provided by an in-game tour guide who
follows the player and gives information about their current location on request and
is the main educational source of the game.

\subsubsection{Deliveries}

Vindolanda is famous for the hundreds of writing tablets recovered from the site
which give valuable insight into life in the fort \cite{adams_new_2003,coles_vindolanda_1985}.
The inclusion of a subset of these was a natural avenue for education by making
the player act as a courier delivering them and able to read the documents. This
was complicated, however, by the poor condition of many tablets which made many
unsuitable for inclusion due to having incomplete translations.

To improve readability of these tablets, only a summary of their text is rendered
on the tablet itself, with the full text being displayed in a larger box above
to allow larger font sizes as required by \hyperref[sec:nfr_text]{NFR03} and depicted
in Figure~\ref{fig:tablet}.

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{tablet.png}
  \caption{\label{fig:tablet}A writing tablet displaying a title on the tablet itself
  and its full context above.}
  \Description{A square wooden writing tablet containing the text ``Letter'', above
  is the contents of the letter in a larger black box.}
\end{figure}

The final delivery system is a simplistic proof of concept: users can speak to
a generic NPC to receive a random tablet, and are given directions on a second
tablet acting as a journal on who to deliver it to. This uses the same quest
system used by the tutorial which allows for future expansion.

\subsection{Tutorial}
Initial user demos were performed by users with no VR experience and no built-in
tutorial. Instead, they were instructed on how to perform tasks such as movement
and archery. This helped inform development of the tutorial required by
\hyperref[sec:fr_tutorial]{FR06}. For example, the Index controllers were less
intuitive than anticipated: users would instinctively grip the controllers
instead of holding with an open palm as intended by the controller design, which
would be registered as attempting to pick up an object. When it came time to pick
up the bow or an arrow in the demo, users struggled to do so and required
assistance.

The basic tutorial was designed with the following stages and is presented non-diegeticlly,
as it focuses on the use of VR systems such as the controllers VR rather than in-game interactions:
\begin{itemize}
  \item Introduction - State the purpose of the tutorial, instruct players on how
    to repeat instructions, advise them on motion sickness, and warn about their
    play area bounds.
  \item Movement - Players move to a target marker using their selected method
    (teleport or smooth) to get them used to basic locomotion.
  \item Interaction - Players pick up an item from a table to teach them how to do
    so, a motion-captured animation is included to demonstrate how this is done.
  \item Menus - Players are instructed to open the game's menu and save their progress
    to teach them what to do when they need a break.
\end{itemize}

Afterwards, players can practice archery with a diegetic teacher. This section assumes
users are already familiar with basic interaction and thus uses terms that maintain
immersion, for example, instructing to ``pick up an arrow from the quiver on your back'' rather than
``grab your controller behind your back to take an arrow''. This teaches more complex interactions
as using a bow requires the multi-step process of taking an arrow, nocking it, drawing the string,
aiming, and releasing. As with to the basic tutorial, a motion-captured NPC is shown using a bow to
demonstrate this motion.

\subsection{Inverse Kinematics and Motion Capture}

To enhance immersion, the player's head, hand, and finger movements are mapped
onto a virtual body using Unity's inverse kinematics (IK) and VR finger tracking.
While controller positions could be directly mapped into IK, finger tracking required
manually adjusting finger bones between rigging and skinning to replace their
animation-driven positions with controller-derived ones.

Initially, the player's animator had root motion enabled, causing it to move independently
of the camera - giving it the appearance of running away as shown in
Figure~\ref{fig:oops_camera}. Disabling root motion solved this issue and resulted
in IK working as expected, shown in Figure~\ref{fig:ik_good}.

\begin{figure}[ht]
  \includegraphics[width=0.5\linewidth]{out-of-body.png}
  \caption{
    \label{fig:oops_camera}The player model running away from the camera.
    \url{https://www.youtube.com/watch?v=W9Mfbanlegs}}
  \Description{A screenshot depicting the player model leaving the camera behind.}
\end{figure}

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{arm-ik.png}
  \caption{\label{fig:ik_good}The player's hands and arms correctly postioned by inverse kinematics.}
  \Description{A screenshot depecting the player's hands being tracked by IK.}
\end{figure}

Unity's hand tracking uses a different and incompatible skeleton to humanoid animations which
required a set of fine-tuned transformations to convert between. These were defined
as part of a scriptable object rather than hard-coded allowing they could be tweaked
at runtime for quicker iteration; this was essential as even a milimeter of error could be
noticeable by users as deformation in their hand meshes. The final hand tracking
system, and the flawed initial version, is shown in Figure~\ref{fig:finger_tracking}

\begin{figure}[ht]
  \subfloat[\centering Incorrectly Positioned Fingers]{\includegraphics[width=0.5\linewidth]{finger-bad.png}}
  \subfloat[\centering Finger Tracking]{\includegraphics[width=0.5\linewidth]{finger-good.png}}
  \caption{\label{fig:finger_tracking}Left: Hand deformation caused by incorrect finger transformations. Right: Accurately tracked fingers mapped onto the user's hand mesh.}
  \Description{Screenshots of the user's hand mesh, the left showing fingers deformed to the left
  while the right shows them correctly positioned.}
\end{figure}

Motion capture is heavily used in the film and games industries to cheaply produce
realistic animations. While this traditionally requires expensive equipment to produce, including
specialised suits or multiple cameras \cite{nogueira_motion_2011}, virtual reality can imitate
this as it provides three highly accurate tracking points: the player's head and their hands.
The Index controllers used by the developer additionally track individual finger states that
further enhance detail.

When combined with IK, which was already implemented to position the player's body,
this results in a rudimentary yet reasonably effective motion capture system, though as expected it
sacrifices precision, making it less effective than a professional system. \cite{vox_evaluation_2021}
This is especially visible in motions such as pulling a hand to the chin to draw a bow, where IK
positioned the elbow in front of the face rather than behind it as shown in Figure~\ref{fig:hand_wrong}

\begin{figure}[ht]
  \includegraphics[width=0.5\linewidth]{mocap-hand-wrong.png}
  \caption{\label{fig:hand_wrong}An incorrectly positioned elbow from motion capture.}
  \Description{A screenshot showing a soldier using a bow with their draw arm's elbow
  appearing in front of their face rather than behind it.}
\end{figure}

\subsection{Mechanics}

\subsubsection{\label{sec:footsteps}Footsteps}

The implementation of footstep sounds uses a combination of systems. First,
movement animations send events when a foot touches the ground. These are listened
to by a script attached to each NPC which performs a raycast from the
foot, specified in the event, to determine which physics material it landed on.
Normally, Unity's physics materials only define friction and bounciness; these were
extended with a \texttt{MaterialData} system that maps materials to extra data
holding a set of footstep sounds. The event listener script uses this data along
with the NPC's current speed to select whether to play a walking or running sound,
and play a randomly selected sound based on material and speed.

\subsubsection{Dialogue}

The fort tour required a significant amount of dialogue to implement. While in
a flatscreen game this could be performed using text only, too much reading could
cause eye strain in VR. While human-voiced dialogue would be higher quality, recording
and editing it would be impractical due to time constraints. Instead, text to
speech (TTS) was used for the vast majority of dialogue. This is largely automated
in a three step process:

\begin{enumerate}
  \item Use a menu item to export dialogue text, ID's, and whether it has a recording to CSV.
  \item Run a Python script that generates TTS using the Google Cloud API.
        An alternative, but more expensive, API for this would have been ElevenLabs.
  \item Using another menu item, import the generated MP3s and link them to dialogue.
\end{enumerate}

This system has two limitations: first is that dialogue does not indicate its speaker,
requiring that all characters use the same voice. This was deemed acceptable as
most lines are spoken by the tour guide, but could be resolved by tagging entries
with a speaker. Second, there is no automatic detection of outdated audio
meaning subtitles and speech could differ if text was edited after audio generation.
This was also acceptable as lines were unlikely to be edited after being written, but
could be resolved by storing timestamps, then regenerating
if text is newer than audio.

Storing dialogue text in-engine made adding subtitles trivial. Speakers request
for a subtitle panel to be displayed when they begin a line and hide it when finished.
The panel, shown in Figure~\ref{fig:subtitles} displays this text alongside the speaker's name in front of the user,
unless subtitles are disabled in which case the request is ignored.

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{subtitles.png}
  \caption{\label{fig:subtitles}The subtitle panel displayed during the tutorial.}
  \Description{A floating subtitle panel displaying the text ``Tutorial: Use the
  right stick to move to the glowing red marker.''}
\end{figure}

While VR motion capture was used for body animations, it is unable to help with
facial animation such as expressions and lip sync. While professional solutions
such as FaceFX are able to produce this from audio alone, they are prohibitavly
expensive for this project. Instead, a simplistic animation of characters moving
their mouth is used with no attempt made at synchronising with speech.

\subsubsection{Archery}

While it was not standard equipment, Roman legions could include archers
\cite{ureche_bow_2013,mcallister_formidable_1993} with recent findings confirming
their existence at the nearby Housesteads fort \cite{collins_soldiers_2013}. This
made it an attractive addition to the game. Implementing this involved several
techniques to both ease development and increase realism.

After picking up a bow, the user's next step is of course to take an arrow from
the ``quiver'', which is in fact a trigger behind the user's back that spawns an
arrow when grabbed. When nocking the arrow, the held item is hidden and replaced
with a separate script-controlled mesh attached to the bow to allow greater control
over its position as the user draws. Upon release, the held arrow is deleted and
replaced with a newly created projectile at a speed determined by draw level.

Each frame, launched projectiles check for colliders in front of them and, if found,
sends an \texttt{OnHit} event to any target components found. After this the projectile
stops checking for more targets and freezes itself in position if the hit material
allows for it to embed using the \texttt{MaterialData} system described in Section~\ref{sec:footsteps}

The bow itself is animated using a vertex animation texture to create a more
realistic effect where the body of the bow bends when drawn, shown in Figure~\ref{fig:vat}.
This is more physically accurate than what is seen in many games which typically
depict the bowstring stretching.

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{bow-loose.png}
  \includegraphics[width=\linewidth]{bow-drawn.png}
  \caption{\label{fig:vat}Top: A bow in its default state. Bottom: A fully drawn bow.}
  \Description{A composite bow, undrawn on top and fully drawn on bottom.}
\end{figure}

\subsubsection{Save and Load}

\begin{figure}[ht]
  % Inkscape can't render Mermaid SVGs :(
  \includegraphics[width=\linewidth]{saveload.png}
  \caption{\label{fig:saveload}Class diagram for the save and load system.}
  \Description{
    A UML class diagram for the save and load system. Saveable inherits from IdOwner,
    IdManager depends on IdOwner, and SaveLoad depends on both Saveable and IdManager.
  }
\end{figure}

Unity, like most general-purpose game engines, does not natively provide a
save/load system required by \hyperref[sec:fr_save]{FR07} , meaning that it had
to be implemented separately. In the editor, any object that to be either saved
or referenced in a save, such as NPCs and patrol points, is automatically assigned
a unique ID that can be safely referenced in a save. During startup, these IDs are
collected for quick mapping during loading of a save.

Any object to be included in a save has a component deriving from the
\texttt{Saveable} class allowing it to inherit and extend base behaviour such
as position and rotation tracking. It also defines two functions to save to an
intermediate \texttt{SaveData} class and restore values from it.

When saving, all saveable objects are gathered and have their save functions invoked.
The results of these operations are serialised as a JSON array containing object
IDs and data for later use. To load a save, the JSON data is decoded and passed
to all serialised objects' load functions to restore their states and resolve
references using the ID map.

This system is limited in that it has no support for tracking dynamically allocated
objects such as fired arrows. Adding support for this would require tracking the
prefab from which the object was instantiated. This would require a distinct system
because Unity's file system does not include paths at runtime. Dynamically created
objects could be identified by the usage of a template prefab but would require
additional work to track and clean up after loading. This was not considered
necessary for \hyperref[sec:fr_save]{FR07} as no significant use cases were found.

\subsection{Requirement Analysis}

A requirement analysis using the MoSCoW method (Must-have, Should-have,
Could-have, and Won't-have) was performed to determine functional and
non-functional requirements for the game.

\subsubsection{Functional requirements}

\paragraph{\label{sec:fr_recreate}FR01 Recreate Vindolanda}
The game \textbf{must} include a recreation of Vindolanda during Hadrian's
rule. This was the primary goal of the proposal.

\paragraph{\label{sec:fr_wall}FR02 Recreate Hadrian's Wall}
The game \textbf{should} include a recreation of Hadrian's wall during Hadrian's
rule. This can take the form of a milecastle.

\paragraph{\label{sec:fr_beforeafter}FR03 Before and After}
The game \textbf{could} provide both past and present depictions of locations.

\paragraph{\label{sec:fr_teleport}FR04 Teleport Locomotion}
The game \textbf{must} include teleport-based locomotion. This is the most
accessible form of movement for new users.

\paragraph{\label{sec:fr_smooth}FR05 Smooth Locomotion}
The game \textbf{should} include smooth locomotion. This is a nice to have for
users more experienced in VR.

\paragraph{\label{sec:fr_tutorial}FR06 Tutorial}
The game \textbf{should} begin with a brief tutorial on how to move around
using the selected locomotion method, and advise them on the motion sickness
they may experience from smooth locomotion. This serves to introduce users to
the basics of navigating a VR world.

\paragraph{\label{sec:fr_save}FR07 Save Progress}
Given how virtual reality can cause motion sickness which require frequent
breaks, there \textbf{should} be a system to save the user's progress and return
later.

\paragraph{\label{sec:fr_lefty}FR08 Left Handed Support}
The game \textbf{must} support both left and right-handed interaction in order
to allow comfortable use by all users. As I am left-handed myself, this priority
is elevated from should-have to must-have.

\paragraph{\label{sec:fr_mocap}FR09 Motion Capture}
The game \textbf{could} use VR input, combined with inverse kinematics, to
create basic motion-captured animations. These \textbf{could} include the ability
to mirror said animations such that I could comfortably record them for right-handed
characters in conjunction with \hyperref[sec:fr_lefty]{FR08}

\subsubsection{Non-functional requirements}

\paragraph{\label{sec:nfr_performance}NFR01 Performance}
The game \textbf{must} maintain a minimum of $45FPS$, $22.2ms$ per frame, at all
times, including during heavy load, to avoid motion sickness.

The game \textbf{should} target $90FPS$, $11.1ms$ per frame, during typical
gameplay to match the HTC Vive's native refresh rate without the need for
reprojection.

\paragraph{\label{sec:nfr_linux}NFR02 Linux Support}
The game \textbf{could} include native Linux support. This is a nice to have as
Proton is good enough for most games and getting SteamVR to work on Linux can be
difficult.

\paragraph{\label{sec:nfr_text}NFR03 Text Legibility}
Text \textbf{must} be displayed using a clear, easily readable font with high
contrast and a size of at least 25mm per metre of viewing distance to ensure
comfortable reading on the widest range of headsets, as discussed in Section
\ref{sec:text_scale}.

\paragraph{\label{sec:nfr_subtitle}NFR04 Subtitles}
All dialogue \textbf{must} provide subtitles for the player to enhance accessibility.
These \textbf{could} be toggleable for the end-user, but should be enabled by default
and opt-out for players who do not want them.

\section{Results \& Evaluation}

\subsection{Unity's Suitability}
Despite being the de facto standard for VR, Unity's implementation of it and
other areas left plenty to be desired and technical debt. For example, its
behaviour graph system requires distinct and incompatible events compared to the
rest of the engine which in turn required wrappers to adapt between. This
introduced extra complexity and reduced overall code quality. Additionally, the
incompatibility between its URP and HDRP pipelines required manual conversion of
assets designed for one to work with the other.

Considering these limitations, the game may have benefited from instead using
Godot, the second engine considered, as its asset library already includes
needed systems such as quests, dialogue, and more flexible behaviour trees that
could be used outside of AI.

\subsection{Performance}

Unity's occlusion culling system, which disables rendering objects blocked by others,
was the main technique used for optimising performance as geometry used fairly low-poly
assets that would not benefit from level of detail. However, historical accuracy
to the layout of the fort remained a priority which limited opportunities to close
sight lines for more efficient culling.

Benchmarking performance for \hyperref[sec:nfr_performance]{NFR01} on desktop PCs
is inherently challenging due to the wide variety of user hardware compared to consoles
and handhelds, such as the PSVR and Meta Quest, which have a limited set of configurations.
Benchmarks were conducted on the developer's system equipped with a high-end GeForce RTX 3070
graphics card.

To approximate performance on lower-end GPUs, these benchmarks were run with a
resolution scaling factor of 400\% (four times as many pixels as normal),
significantly increasing GPU load. It should be noted that resolution scaling
is expected to have minimal CPU impact, and thus will not detect any CPU-bound
issues.

SteamVR's built-in performance graph, shown in Figure~\ref{fig:vr_profile} and
throughout the demo video (\url{https://youtu.be/CaM-xLr3iG0}) was used to display frame
times during gameplay. This data can be recorded for later review and correlated
with gameplay to identify areas with performance issues. It should be noted however,
that recording video adds a non-negligible GPU overhead that could affect results
in GPU-bound scenarios, a condition that appears to apply for most of the game.

Unexpectedly, the area with the highest frame times was Hadrian's Wall and Castle Nick
where frame times reached up to $21ms$, despite this region seemingly having
significantly less visible geometry than the fort. Further investigation suggested
two contributing factors. First, although the fort was fully culled by Unity's
occlusion system, it still incurred some overhead, though profiling proved inconclusive.
Second, the grass, used much more extensively around the wall, caused substantial
overdraw, straining VRAM bandwidth with a potentially exaggerated impact due to the
aforementioned 400\% resolution scaling. Other than this, frame times hovered at
an average of $10-12ms$, close to the target of $11ms$.

\begin{figure}[ht]
  \includegraphics{vr-profile.png}
  \caption{
    \label{fig:vr_profile}SteamVR's performance profiler showing frame times
    over the past 2 seconds.}
  \Description{
    A screenshot of SteamVR's performance profiler showing an average
    frame time of 9ms and most frames completed below the 11ms target.}
\end{figure}

\section{Conclusion \& Future Work}
Through creation of a VR fort, tour, and basic delivery system, this paper establishes
a baseline for further study into the applications of virtual reality for education,
particularly in relation to the Vindolanda fort. While VR is an inherently demanding
platform, the game would be capable of running on more modest hardware than what
the developer had access to. Key limitations of this study include limited
user testing, especially with the final tutorial, and quest system that, while
sufficient, lacks flexibility. Future work would focus on upgrading the quest
system to support more varied content and deepen its educational value, and
expanding the fort to include more detailed areas and characters for use in such
quests.
% TODO: Acknowledgments
\begin{acks}

\end{acks}

\section*{Datasets}
{\raggedright
  Terrain height maps and textures sourced from DEM.Net Elevation API

  Generator: DEM Net Elevation API -
  \url{https://elevationapi.com}

  Mesh reduction: geometry3Sharp (GradientSpace, Ryan Schmidt) BSL 1.0 License -
  \url{https://github.com/gradientspace/geometry3Sharp/blob/master/LICENSE}

  Digital Elevation Model: SRTM\textunderscore{}GL3 OpenTopography -
  \url{https://opentopography.org/}

  Imagery: MapBox Satellite -
  \url{https://www.mapbox.com}

  Landscape references via OpenStreetMap -\\
  \url{https://www.openstreetmap.org/export}
}

% Enable if "You have called \balance in second column" appears
% \balance
\bibliographystyle{ACM-Reference-Format}
\bibliography{vindolanda}

\section*{Additional Sources}
Birley, Andrew. 2023. ``The Vindolanda Guidebook''. Vindolanda Trust, no ISBN. Available
at: \url{https://www.vindolanda.com/shop/vindolanda-guide}

Writing tablet texts and translations sourced from Roman Inscriptions of Britain
under the Creative Commons 4.0 International License
\url{https://romaninscriptionsofbritain.org/about/terms-of-use}

\appendix

\end{document}
